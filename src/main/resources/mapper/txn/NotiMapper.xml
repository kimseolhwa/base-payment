<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.github.shkim.base.noti.mapper.NotiMapper">

    <insert id="insertNotiHistory" parameterType="com.github.shkim.base.noti.dto.NotiRequests$SendReq">
        INSERT INTO tb_noti_history (
                                      merchant_id
                                    , order_id
                                    , amount
                                    , status
                                    , send_dt
        ) VALUES (
                   #{merchantId}
                 , #{orderId}
                 , #{amount}
                 , #{status}
                 , CURRENT_TIMESTAMP
                 )
    </insert>

</mapper>